machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t stephengfriend/cmsbl-este:$CIRCLE_SHA1 .

test:
  override:
    - docker run stephengfriend/cmsbl-este:$CIRCLE_SHA1 test
